# Z-Index Fix for OO.ui Dialogs

## المشكلة
عند النقر على زر "GO" أو "Close"، كانت نافذة التأكيد من OO.ui تظهر خلف النافذة الرئيسية لـ Category Batch Manager ولا يمكن الوصول إليها.

## السبب
- Category Batch Manager كان يستخدم `z-index: 10000`
- نوافذ OO.ui تستخدم `z-index` في نطاق 1000-2000
- النتيجة: نافذة التأكيد تظهر خلف النافذة الرئيسية

## الحل

### تخفيض Z-Index لجميع العناصر

#### 1. النافذة الرئيسية (main.css)
```css
.cbm-container {
  z-index: 100;  /* كانت: 10000 */
}
```

#### 2. نافذة المعاينة (main.css)
```css
.cbm-modal {
  z-index: 101;  /* كانت: 10001 */
}
```

#### 3. زر إعادة الفتح (CategoryBatchManagerUI.js)
```javascript
reopenBtn.style.cssText = 'position: fixed; bottom: 20px; right: 20px; z-index: 99; display: none;';
// كان: z-index: 9999
```

## ترتيب Z-Index النهائي

| العنصر | Z-Index | الوصف |
|--------|---------|-------|
| Reopen Button | 99 | زر إعادة فتح النافذة |
| Main Container | 100 | النافذة الرئيسية |
| Preview Modal | 101 | نافذة معاينة التغييرات |
| OO.ui Dialogs | ~1000-2000 | نوافذ التأكيد (الأعلى) |

## الفوائد

✅ **نوافذ التأكيد تظهر فوق كل شيء**
- يمكن الوصول إليها دائماً
- لا تُحجب بواسطة عناصر أخرى

✅ **ترتيب منطقي**
- العناصر الأقل أهمية (reopen button) في الأسفل
- العناصر الأكثر أهمية (dialogs) في الأعلى

✅ **تجنب التعارضات**
- z-index منخفض لا يتعارض مع عناصر MediaWiki الأخرى
- مساحة كافية لنوافذ OO.ui

## الملفات المُعدّلة

1. ✅ `src/ui/styles/main.css` - تخفيض z-index للـ container والـ modal
2. ✅ `src/ui/CategoryBatchManagerUI.js` - تخفيض z-index لزر reopen
3. ✅ `docs/OOUI_DIALOG_INTEGRATION.md` - توثيق المشكلة والحل

## الاختبار

### قبل الإصلاح ❌
```
User clicks "GO" → Dialog appears behind main window → User can't access dialog
```

### بعد الإصلاح ✅
```
User clicks "GO" → Dialog appears on top → User can confirm/cancel → Works perfectly
```

## ملاحظات مهمة

### ⚠️ لماذا لم نستخدم z-index أعلى؟
- نوافذ OO.ui **محمية** بـ WindowManager الذي يدير z-index تلقائياً
- محاولة استخدام z-index أعلى من 10000 قد تتعارض مع عناصر أخرى في MediaWiki
- الحل الأمثل هو إبقاء z-index منخفض للسماح لـ OO.ui بالعمل بشكل صحيح

### 💡 Best Practices
- استخدم z-index < 1000 لعناصر التطبيق العادية
- اترك نطاق 1000+ لنوافذ النظام (modals, dialogs)
- لا تستخدم z-index عالي جداً (>10000) إلا إذا كان ضرورياً جداً

## التاريخ
- **المشكلة**: 7 فبراير 2026 - Dialog يظهر خلف النافذة
- **الإصلاح**: 7 فبراير 2026 - تخفيض z-index إلى 100
- **الحالة**: ✅ تم الحل
